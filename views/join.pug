extends layouts/main.pug

block content
    .form-container
        form(action=routes.join, method="post")
            input(type="text", name="name", placeholder="full name", required=ture)
            input(type="email", name="email", placeholder="email", required=ture)
            input(type="password", name="password", placeholder="verify password", required=ture)
            input(type="password", name="password2", placeholder="verify password", required=ture)            
            input(type="submit", value="Join Now")
        include partials/socialLogin